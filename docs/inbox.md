# 需求收件箱

> 原始想法、改进提议、架构疑虑在此登记。评估后路由到版本计划(plan)、试验记录(exp)、架构决策(ADR)、或关闭。
> 所有需求/决策/扩展提议的唯一入口——不要直接从 AGENTS.md 活跃待办或 ADR 起步。

## 路由规则

评估后按以下规则分流：

| 场景 | 路由目标 | 说明 |
|------|------|------|
| 新功能、功能改进 | → `docs/plan-*.md` checklist | 版本计划轨道 |
| 模块接口扩展（新增 API、类型签名变更） | → `docs/plan-*.md` checklist | 消解为 plan 的普通条目 |
| **重大架构变更/技术选型变化** | → 先建 `docs/adr/ADR-NNN.md` → plan 引用 | 需要记录决策前因(why)时 |
| 暂不确定方向 | → 保留在 inbox 待评估（标注暂缓原因） | 问题明确、方向已知，但依赖不成熟 |
| 试验性工作（探针、对照、测量、诊断、技术评估、历史运行分析、答案核查） | → `docs/exp-*.md` | 按 exp 模板记录过程、结果、结论；形成工程交付另建 plan，架构决策按判据建 ADR，不额外保留试验附件 |
| 明确不采纳 | → 已关闭（记录理由） | — |

### ADR 触发判据

**需要建 ADR（正向）**：新依赖引入、跨模块公共接口签名变更、分发/部署策略变更、废弃旧方案（需替代理由）。

**直接进 plan（反向）**：单模块内部重构、配置默认值调整、渐进式重构不改变公共接口、Bug 修复。

> 不确定是否需要 ADR 时，先写入 plan checklist。如果条目需要解释"为什么这样做"再补 ADR。

## 待办区

- [ ] **查询工具链收敛与预算语义调整** — 默认模式改为 hybrid（rag_search + read_section + facts_search），移除 grep_search 工具与 grep/both/facts 三个模式；工具预算改为「仅非空执行成功结果扣额度，默认 5 次成功额度 + 10 次获准尝试硬上限」，空结果及错误免扣成功额度，批内逐项串行结算；保留历史格式读取与既有循环终止边界。见 plan-hybrid-default-tool-budget.md、ADR-011、ADR-012。 — 2026-09-10 — 已路由 plan；2026-09-11 按可行性评估修订
- [ ] **选择后续性能试验候选** — archive/exp/exp-harness-performance.md 已归档，仅保留实测与覆盖诊断、未保留候选；当前未选下一项试验，重启时另行立项并明确范围与输入，不沿用已清理的附件，不自动启动优化或付费运行。 — 2026-09-09
- [ ] **评估语料实体标记的小规模验证** — 针对名称改写与干员/阵营混用，设计固定检索片段的原文、边界标记、类型标记三组对照，见 exp-entity-marking-probe.md；原设计基于 lookup/query_operators 路由，当前已统一为 facts_search，重启前需按 schema v5 重估。仅评估，不修改正式语料或启动付费调用。 — 2026-09-07
- [ ] **补充组合／搭配名称结构与表达标准（暂缓）** — 以“阿兰娜＋温米”等表达为线索，后续评估正式组合准入、规范名结构、成员关系描述及推荐边界，并核对现有组合、语料旧名和运行时条件的一致性。用户因范围偏大决定本轮不做，仅保留 inbox 待办；重启时先明确范围与命名规则，再决定语料、登记和检索词典的迁移，不把所有具名搭配归为旧称，不改写历史核查结论。 — 2026-09-10
- [ ] **为 exp 试验流程增加机械校验，降低非机械复核量** — 本次 GLM 20 题核查提交经 5 轮独立复审（技能规定内容变化即须换新审查者），发现问题多为表格漏行、表与正文不一致、计数与口径不统一、措辞与图例等本可机械检出的自洽性问题；当前 exp 模板与 doc-check 仅覆盖三段结构、归档位置与引用有效性，缺少对计数自洽与表格一致性的门禁，复核成本大量落在人工与子代理。拟评估：为 exp 增设可机械校验的约束（必答项计数与表格行数一致性、关键聚合数值的落盘与可复算校验、状态标记与图例的表格符号约定），并划清哪些检查可进 doc-check、哪些仍须人工；仅在证据表明能减少复核成本时实施，不强制所有 exp 采用。 — 2026-09-10 — 先评估，可能建 plan 或扩展 doc-check
- [x] **文档规范三项收敛：引用写名称、放宽非语义二次复审、固定 exp 模板结构** — 引用一律写名称、不再用 Markdown 链接（ADR 索引与归档索引表格为机械契约例外），避免归档与重命名引发链接回改；commit-convention 对非逻辑性、非阻塞改动放宽二次审查，可沿用原审查结论、无需重新派生审查者，涉及行为/契约/数据/结论的改动仍完整复审；exp 模板固定头部元数据定序与 过程/结果/结论 三段，为后续机械校验铺路（与上条 exp 机械校验评估衔接，本条只做结构铺垫）。 — 2026-09-10

<!-- 格式: - [ ] **简短标题** — 描述 — 提出日期 — 可能路由 -->
<!-- 完成后标记 [x]，发版时清理已完成条目；定期（如每季度）治理重估：确认暂缓原因仍成立、路由目标未悬空 -->
