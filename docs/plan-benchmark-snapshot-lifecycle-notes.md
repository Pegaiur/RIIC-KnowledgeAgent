# 实施笔记：共享基准快照更新与淘汰

> 对应 spec：[实施计划](plan-benchmark-snapshot-lifecycle.md)
> 开始日期：2026-09-06

## 决策偏离

暂无。以计划规定的严进宽出为核心，复用现有文档、快照枚举及提交流程。

## 实现调整

已在 `bench/src/benchmark-integrity.ts` 的既有共享快照枚举中累加有效 JSON 字节，并让 `bench validate` 同时输出快照数量和总字节；`bench/results` 缺失或没有 JSON 时返回 `0 份、0 字节`，非空目录仍逐份调用 `readSnapshot` 严格校验。测试覆盖真实非空目录和隔离的缺失/空目录。

已在 `docs/rules/document-lifecycle.md` 落入严进宽出、用途核对和完整提交号定位规则；`skills/release-workflow/SKILL.md` 只补收尾时机和操作，`scripts/INDEX.md` 只补规则引用。`docs/spec/rag-answer-baseline.md` 明确当前正式质量基线未指定，并列出三份仍有用途的运行参考。

## 债务记录

暂无新增债务。

## 意外发现

### 2026-09-06 — 计划审查补齐零快照退出

独立计划审查指出现有 validate 强制至少一份快照，阻止最后一份无用途结果退出。已在计划中明确空集合返回 0 份、0 字节，非空仍严格校验，并补充 build 后执行 CLI 验证；最终复审 PASS。

## 阻塞与解决

### 2026-09-06 — 计划交接准备

计划审查通过后建立本笔记；6 项验收尚未实施，保持未勾选。doc-check 当前仅有该计划的 6 个 D1，须在实施验收后消解。

沿用 `feature/temp-cleanup` 与当前本地目录。前一轮清理修复和验收文档尚未提交，作为当前工作区基底保留；本次计划提交不包含它们。后续实施须保留这些变更，若需提交，按单一关注点分别走提交技能。生产原件、付费跑测、合并及推送均不在本次实施范围。

### 2026-09-06 — 快照去留与历史定位

- 入库前核对现有 4 份实际记录：`2026-09-05T10-51-01-118Z-qwen-off-t0.json` 和 `2026-09-05T10-54-08-021Z-qwen-off-t0.json` 分别保留为 2 点、5 点预算比较；`2026-09-05T10-59-42-129Z-qwen-off-t0.json` 保留为旧实现参考。`2026-09-01T12-06-28-412Z-qwen-low.json` 为 20 题、0 records 的 API 失败对照，当前活动文档没有引用，本次不再保留。
- 该淘汰文件实际包含于提交 `47457411b88326a2305e7bba28bf55b6f657d076` 的 `bench/results/2026-09-01T12-06-28-412Z-qwen-low.json`；已用 `git show <commit>:<path>` 验证可恢复。没有待执行清理清单依赖该快照，未删除任何生产 `bench-runs` 原件。

### 2026-09-06 — 隔离验收范围

在隔离临时目录准备新旧快照和一份历史恢复副本，验证新快照替换当前引用、旧快照从 Git 恢复后可被 `readSnapshot`/`report` 读取，且恢复前后聚合数据一致；同时核对当前 spec 只引用三份保留样本。该验证不发起 LLM 请求、不修改生产临时目录。

### 2026-09-06 — 实施验收完成

- `pnpm run typecheck`、`pnpm run test`（31 个测试文件、265 项测试）、`pnpm run build` 和构建后 `node dist/cli.js validate` 均通过；validate 报告当前 3 个共享快照、313133 字节。
- 隔离验收最终通过：新快照替换旧引用、历史 JSON 从 `47457411b88326a2305e7bba28bf55b6f657d076` 恢复、恢复前后 20 题聚合数据一致，`report` 可读取恢复文件。第一次探针因误判报告标题失败，读取实际 CLI 输出后修正断言并重跑通过，未涉及产品代码缺陷。
- 计划 6 项验收据实勾选；本次只移除已确认无当前用途的已跟踪失败快照，未删除生产 `bench-runs` 原件，未付费补测，未合并、推送或发版。
