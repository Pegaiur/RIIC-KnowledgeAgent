# 实施笔记：ADR-009 exp 文档流程

> 对应 spec：[ADR-009](ADR-009-exp-document-workflow.md)
> 开始日期：2026-09-09

## 决策偏离

无。沿用已审方案；本轮流程治理属于 ADR 实施，不属于 exp 试验，不新增迁移 plan。

## 实现调整

### 2026-09-09 — 开始迁移

- 新增 exp 三段模板、归档目录和 inbox 路由；六份记录按既有过程、结果及结论重组，保留历史小节标题。
- 已有 plan 仅修正 Markdown 链接目标；不更名、不改变正文事实或清单。

### 2026-09-09 — 迁移与清理落地

六份记录已迁移，report类型取消，exp模板与规则/inbox/发布路由已同步。五次历史运行25个原件及5份快照移除，harness诊断11个文件、独立工具副本非依赖文件及本地依赖缓存1005个文件已通过显式清单清理。原清单中三次运行的snapshot依赖和指纹原样沿用，先清理原件，再移除快照。其余历史plan与非本次关联运行未批量整理。

验证：`node scripts/doc-check.mjs`、`pnpm run typecheck`、`pnpm run test`（35文件、347项）及 `git diff --check` 均通过。专项核对确认六份exp均仅有过程/结果/结论三段，既有plan仅链接目标变化，三份核查表及spec「使用口径」之后的判定正文保持一致，迁移未新增失效路径或锚点。原核查记录中51处既有知识源锚点与现行完整标题不一致，作为历史定位保留，未扩张为知识源或核查结论修订。

本轮一次性迁移与核对脚本在 `dev-temp/work/exp-document-migration/`，完成后经显式清单清理，不作为正式工具或试验附件保留。未提交或合并。

### 2026-09-09 — 提交审查发现迁移遗漏

独立审查发现性能exp的20项覆盖判定表未实际写入，只有已保存的说明。已从本任务此前读取原 `holdout-retrieval.md` 的工具输出补回20项原判定及必要摘录，统计仍为15/3/1/1，不重新生成判定或评分。此前专项验证只比较原有表格，没有覆盖新增摘录段是否非空；本次补查该段恰有20个编号且分布一致。修订后的最终内容需重新独立审查后提交。

## 债务记录

无新增代码债务。

## 意外发现

spec的运行观察与附件用途不属于判定规则，已迁至性能exp，逐题核查口径未改。

## 阻塞与解决

现存v4审定输入按ADR一次性过渡安排保留，两次运行各5个输入及2份快照；审定完成或取消即清理。独立工具副本在实施时因junction导致整树清理被拒绝，随后已清理可安全处理的源码/构建/试验文件。提交前再次检查发现 `dev-temp/work/independent-tools/` 整目录已不存在，已将exp及inbox同步为无当前可定位残留；本次核对未重新删除，不推断后续清理者或方式。
