# 实施笔记：提示词与 tool schema 优化

> 对应 spec：docs/plan-prompt-tool-schema.md
> 开始日期：2026-09-07

## 决策偏离

### 2026-09-07 — 按独立审查定稿并交接实施
- **背景**：用户要求独立审查后按意见形成计划，再交接 Luna/xhigh 新任务，直接使用仓库。
- **决策**：草案重命名为正式计划；保留当前分支，交接时不创建工作树。新任务依计划完成实现与本地验证，不追加付费实验。
- **审查修订**：补五模式缺失工具回退；明确拼接字符串合法空查与参数拒绝的区别；补旧1–10条映射和V01–V06输入/预期；区分mock宿主可继续与真实模型决策。schema版本4、facts结果版本1，旧记录不回填。
- **复审结论**：独立agent `review_prompt_schema` 返回PASS，可交接实施、无阻塞意见；非阻塞的验证章节层级建议也已采纳。
- **影响**：当前验收条目保持未勾选；实施完成后由worker据实际验证勾选，不为通过文档门禁提前标记完成。

## 实现调整

### 2026-09-07 — 完成描述、提示词与确定性覆盖
- **工具 schema**：`lookup` 与 `query_operators` 仅校正能力、字段语义和缺失工具边界；保留既有输入结构、校验器、匹配实现和结果协议。`TOOL_SCHEMA_VERSION` 从3升至4，`FACTS_RESULT_VERSION` 保持1，版本指纹继续由实际工具数组计算。
- **人工指令**：将旧1–10条合并为7条，保留领域限制、对象/设施取证边界、数值与组合判断约束、中文作答和未知声明；新增按证据缺口路由、缺失工具回退、已有证据足够时不重复查询，以及空查后的继续/停止条件。
- **运行能力块**：预算说明改为动态预算、每个准入调用计1点、参数错误计点、同批分别计费和余额用尽后不再执行新增调用；未改变宿主预算逻辑。
- **测试覆盖**：补充连续含空格短语、拼接名称合法空查、可选字段非法值与 `excludeIds:[]` 合法路径；更新 schema 版本断言。未增加整段提示词快照。

### 2026-09-07 — 本地验证与产物收尾
- **确定性验证**：计划定向测试 6 个文件、109 个测试通过；全量 `typecheck`、293 个测试和 `build` 均通过。
- **模式 dry**：`bm25`、`grep`、`both`、`facts`、`hybrid` 各运行2题 dry，均完成；该结果只证明构建与五模式宿主路径可运行，不作为真实模型行为或质量改善证据。
- **文档与差异**：`node scripts/doc-check.mjs`、`git diff --check` 通过；计划验收清单据实际结果勾选完成，未冻结或归档计划。
- **临时产物**：五模式 dry 目录共25个文件、170778字节，按 `scripts/INDEX.md` 通过显式清单清理，清单随成功执行移除；未新增正式基准快照。

## 债务记录

## 意外发现

### 2026-09-07 — 仓库 git 配置文件不可写
- **现象**：按仓库首次 git 操作约定写入 `.git/config` 时权限不足，三项编码配置均未能落盘。
- **影响**：本轮不提交、不合并，未影响代码、测试或文档验证；保留该环境限制供后续需要提交的任务处理。

## 阻塞与解决

### 2026-09-07 — 计划交接时的文档门禁状态
- **症状**：doc-check 返回7条D1，全部对应本计划尚未实施的7项验收条目，未报告其它问题；差异空白检查通过。
- **原因**：该检查按合并/发版收束口径要求活动计划全部完成，当前仅定稿准备施工。
- **处理**：如实保留未勾选条目并交接；worker完成实施与验证后再运行门禁。此状态不阻止定稿交接，也不通过改脚本或提前勾选规避。
